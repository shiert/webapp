// 系统配置
export const config = {
  // 系统名称-最好不要超过10字
  systemName: '贵州习水人事招生系统',
  // 接口地址
  baseURL: 'http://127.0.0.1/cosy/'
}

// 导出配置项
export const { systemName, baseURL } = config

// 默认导出整个配置对象
export default config
